// 原始类型
let bool: boolean = false

let add = (x: number, y: number) => x + y

let compute: (x: number, y: number) => number
compute = (a, b) => a + b

let num: number  | undefined | null= 3

num = null